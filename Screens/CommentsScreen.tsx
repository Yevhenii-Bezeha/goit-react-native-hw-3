import { SafeAreaView, Text } from "react-native";

type CommentsScreenProps = {};

export default function CommentsScreen({}: CommentsScreenProps) {
  return (
    <SafeAreaView>
      <Text>CommentsScreen</Text>
    </SafeAreaView>
  );
}
