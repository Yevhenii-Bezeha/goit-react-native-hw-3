import { SafeAreaView, Text } from "react-native";

type MapScreenProps = {};

export default function MapScreen({}: MapScreenProps) {
  return (
    <SafeAreaView>
      <Text>MapScreen</Text>
    </SafeAreaView>
  );
}
