import { SafeAreaView, Text } from "react-native";

type PostsScreenProps = {};

export default function PostsScreen({}: PostsScreenProps) {
  return (
    <SafeAreaView>
      <Text>PostsScreen</Text>
    </SafeAreaView>
  );
}
